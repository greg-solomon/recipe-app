import Alert from "./Alert";
import CardImage from "./Image";
import Spinner from "./Spinner";
import {
  red,
  blue,
  green,
  purple,
  teal,
  orange,
  grey,
} from "@material-ui/core/colors";

const colors = [
  grey[900],
  red[900],
  blue[900],
  green[700],
  purple[900],
  teal[700],
  orange[700],
];

export { Alert, CardImage, Spinner, colors };
